# v0.8 配置和测试

## 版本信息
- **版本号**: v0.8
- **提交哈希**: 7edaf42

## 模块概述

配置和测试模块是ORM框架的最终交付形态。本模块包含：

1. **resources** - 配置文件
2. **test** - 测试代码

---

## 1. 配置文件

### 1.1 mybatis-config.xml

主配置文件：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/mybatis"/>
                <property name="username" value="root"/>
                <property name="password" value="123456"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <mapper resource="mapper/UserMapper.xml"/>
    </mappers>
</configuration>
```

### 1.2 配置节点说明

| 节点 | 说明 |
|------|------|
| environments | 环境配置 |
| transactionManager | 事务管理器 |
| dataSource | 数据源 |
| mappers | Mapper映射器 |

### 1.3 数据源类型

| 类型 | 说明 |
|------|------|
| UNPOOLED | 非池化 |
| POOLED | 连接池 |
| DRUID | Druid连接池 |
| JNDI | JNDI数据源 |

---

## 2. Mapper XML

### 2.1 基本结构

```xml
<mapper namespace="com.rongx.dao.UserDao">
    <resultMap id="userMap" type="com.rongx.po.User">
        <id column="id" property="id"/>
        <result column="user_name" property="userName"/>
    </resultMap>
    
    <select id="findById" resultMap="userMap">
        SELECT * FROM user WHERE id = #{id}
    </select>
</mapper>
```

### 2.2 SQL元素

| 元素 | 说明 |
|------|------|
| select | 查询 |
| insert | 插入 |
| update | 更新 |
| delete | 删除 |
| resultMap | 结果映射 |
| parameterMap | 参数映射 |

### 2.3 动态SQL

```xml
<select id="findUser">
    SELECT * FROM user
    <where>
        <if test="id != null">
            AND id = #{id}
        </if>
        <if test="name != null">
            AND name = #{name}
        </if>
    </where>
</select>
```

---

## 3. 测试代码

### 3.1 单元测试

**TypeHandlerRegistryTest**:

```java
public class TypeHandlerRegistryTest {
    @Test
    public void testHasTypeHandler() {
        // 测试Integer类型处理器
        assertTrue(registry.hasTypeHandler(Integer.class));
    }
    
    @Test
    public void testGetTypeHandler() {
        TypeHandler<Integer> handler = registry.getTypeHandler(Integer.class, JdbcType.INTEGER);
        assertNotNull(handler);
    }
}
```

**GenericTokenParserTest**:

```java
public class GenericTokenParserTest {
    @Test
    public void testParse() {
        GenericTokenParser parser = new GenericTokenParser("#{", "}", handler);
        String result = parser.parse("SELECT * FROM user WHERE id = #{id}");
        assertEquals("SELECT * FROM user WHERE id = ?", result);
    }
}
```

### 3.2 集成测试

**ApiTest**:

```java
public class ApiTest {
    private SqlSessionFactory factory;
    
    @Before
    public void setUp() throws Exception {
        Reader reader = Resources.getResourceAsReader("mybatis-config.xml");
        factory = new SqlSessionFactoryBuilder().build(reader);
    }
    
    @Test
    public void testQuery() {
        SqlSession session = factory.openSession();
        try {
            UserDao dao = session.getMapper(UserDao.class);
            User user = dao.findById(1);
            assertNotNull(user);
        } finally {
            session.close();
        }
    }
}
```

### 3.3 测试DAO

```java
public interface UserDao {
    @Select("SELECT * FROM user WHERE id = #{id}")
    User findById(Integer id);
    
    @Select("SELECT * FROM user")
    List<User> findAll();
    
    @Insert("INSERT INTO user(name) VALUES(#{name})")
    int insert(User user);
}
```

### 3.4 测试POJO

```java
public class User {
    private Integer id;
    private String name;
    
    public Integer getId() { return id; }
    public void setId(Integer id) { this.id = id; }
    
    public String getName() { return name; }
    public void setName(String name) { this.name = name; }
}
```

---

## 4. 使用流程

### 4.1 创建SqlSessionFactory

```java
Reader reader = Resources.getResourceAsReader("mybatis-config.xml");
SqlSessionFactory factory = new SqlSessionFactoryBuilder().build(reader);
```

### 4.2 获取SqlSession

```java
SqlSession session = factory.openSession();
```

### 4.3 获取Mapper

```java
UserDao dao = session.getMapper(UserDao.class);
```

### 4.4 执行CRUD

```java
// 查询
User user = dao.findById(1);

// 插入
dao.insert(new User("Tom"));

// 更新
user.setName("Jerry");
dao.update(user);

// 删除
dao.delete(1);

// 提交
session.commit();

// 关闭
session.close();
```

---

## 5. 测试覆盖

### 5.1 模块测试

| 测试类 | 测试范围 |
|--------|----------|
| TypeHandlerRegistryTest | 类型处理器 |
| GenericTokenParserTest | SQL解析 |
| PooledDataSourceTest | 连接池 |
| MapperProxyTest | 动态代理 |
| IfSqlNodeTest | 动态SQL |

### 5.2 测试框架

- JUnit 4/5
- 断言: assertEquals, assertNotNull, assertTrue

---

## 6. 面试问题

### Q1: 配置文件加载流程？
**Answer**: Resources → Reader → XMLConfigBuilder → Configuration

### Q2: Mapper接口如何绑定SQL？
**Answer**: MapperAnnotationBuilder解析注解，生成MappedStatement

---

## 7. 总结

配置和测试要点：
1. XML配置集中管理
2. 注解简化Mapper定义
3. 单元+集成测试覆盖